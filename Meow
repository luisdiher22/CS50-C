#include <cs50.h>
#include <stdio.h>
//for(int;condition;int modifier){}
//Declaring a function
void meow(int n)
{
     for(int i = 0; i < n; i++)
        {
         printf("meow\n");
        }

}

int main (void)
{
    meow(10);
}
